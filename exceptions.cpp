#include <iostream>
#include <stdexcept>

// 1:
int main()
{
    std::logic_error;
    std::domain_error;
    std::invalid_argument;
    std::out_of_range;

    catch(std::range_error re)
    {
        std::cout << 
    }
}